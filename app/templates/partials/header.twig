<header class="flex justify-between  w-full ">
  {% include 'partials/header/header-start.twig'%}
  {% include 'partials/header/header-search.twig'%}
  {% include 'partials/header/header-menu.twig'%}
</header>


<script>
  const menuHeader = document.getElementById('menu-header');
  const menuItems2 = document.querySelectorAll('[role="menuitem"]');

  menuHeader.addEventListener('click', () => {
  const expanded = menuHeader.getAttribute('aria-expanded') === 'true' || false;
  menuHeader.setAttribute('aria-expanded', !expanded);
  menuHeader.nextElementSibling.classList.toggle('hidden');
  });

  menuItems2.forEach(item => {
  item.addEventListener('click', () => {
  });
  });
</script>